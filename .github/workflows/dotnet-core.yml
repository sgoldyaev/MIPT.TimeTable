name: .NET Core

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.101
    - name: Install dependencies
      run: dotnet restore projects/MIPT.TimeTable.sln
    - name: Build
      run: dotnet build projects/MIPT.TimeTable.sln --configuration Release --no-restore
    - name: Test
      run: dotnet test projects/MIPT.TimeTable.sln --verbosity=minimal
    - name: Docker Deployment
      uses: wshihadeh/docker-deployment-action@v2
      with:
        remote_docker_host: sgoldyaev@sp.dbeliakov.ru
        ssh_private_key: ${{ secrets.SSH_PRIVATE }}
        ssh_public_key: ${{ secrets.SSH_PUBLIC }}
        deployment_mode: docker-compose
        copy_stack_file: false
        deploy_path: /home/sgoldyaev/timetable
        stack_file_name: docker-compose.yaml
        keep_files: 2
